# Railway sets PORT itself — only needed for local overrides.
PORT=4000
NODE_ENV=development
# Origin allowed to call this API in production (e.g. your Vercel URL).
# Leave unset locally; defaults to * (all origins) if unset.
CORS_ORIGIN=*
